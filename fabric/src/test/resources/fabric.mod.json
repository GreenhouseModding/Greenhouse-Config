{
  "schemaVersion": 1,
  "id": "${mod_id}_test",
  "version": "${mod_version}+${minecraft_version}",
  "name": "${mod_name} Test",
  "description": "${mod_description}",
  "authors": [
    "${mod_author}"
  ],
  "contributors": [
    "${fabric_mod_contributors}"
  ],
  "contact": {
    "homepage": "${homepage}",
    "issues": "${issues}",
    "sources": "${sources}"
  },
  "license": "${mod_license}",
  "environment": "*",
  "entrypoints": {
    "main": [
      "dev.greenhouseteam.greenhouseconfig.test.GreenhouseConfigTestFabric"
    ],
    "client": [
      "dev.greenhouseteam.greenhouseconfig.test.client.GreenhouseConfigTestFabricClient"
    ]
  },
  "depends": {
    "fabricloader": "${fabric_loader_range}",
    "fabric-api": "*",
    "minecraft": "${fabric_minecraft_version_range}",
    "java": ">=${java_version}"
  }
}